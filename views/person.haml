
#person
  %h1= @person['name']
  %ul 
    - @person['memberships'].each do |mem|
      %li<
        - if mem['party']['id']
          <a href="/party/#{mem['party']['id']}">#{mem['party']['name']}</a>
        - else 
          #{mem['party']['name']}

        - if mem['start_date'] or mem['end_date']
          (
        - if mem['start_date'] 
          = mem['start_date'].year
        - if mem['start_date'] or mem['end_date']
          –
        - if mem['end_date'] 
          = mem['end_date'].year
        - if mem['start_date'] or mem['end_date']
          )

  %h3 Stances

  %ul 
    - @stances.sort_by { |s| s['weight'] }.reverse.each do |s| 
      %li voted <span class="stance">#{s['stance_text']}</span> <a href="/issue/#{s['id']}">#{s['text']}</a>

  %h3 Party Agreements

  %ul
    - @party_distances.sort_by { |k,v| v }.each do |p, d|
      %li <a href="/party/#{p['id']}">#{p['name']}</a> #{100-(d * 100).to_i}%

